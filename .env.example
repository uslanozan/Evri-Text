# Optional: used only by integration tests that call the real Gemini API.
# The Android app does not read this file; users enter their own key in the TV UI.
# Create a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
